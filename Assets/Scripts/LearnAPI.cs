using UnityEngine;

namespace KID
{
    /// <summary>
    /// 學習非靜態 API
    /// 與靜態的差異在於非靜態需要實體物件
    /// 實體物件：場景上的物件
    /// </summary>
    public class LearnAPI : MonoBehaviour
    {
        // 屬性
        // 1. 取得

        // 語法：
        // 類別名稱 欄位名稱； - 欄位，儲存實體物件用
        // 欄位名稱.屬性名稱；

        public Transform cube;

        // 2. 設定

        // 方法
        // 3. 使用方法
    }
}

